* text=auto eol=lf

# Never let Git rewrite line endings inside a binary -- it corrupts the
# installer, and the corruption only shows up when somebody tries to run it.
*.exe binary
*.png binary
*.ico binary
